# iPayroll OAuth Configuration
# only required if you want to sync iPayroll data
IPAYROLL_CLIENT_ID="your-client-id"
IPAYROLL_CLIENT_SECRET="your-client-secret"

# Jira Integration (https://id.atlassian.com/manage-profile/security/api-tokens)
# only required if you want to sync Jira data
JIRA_API_TOKEN="your-jira-api-token"
JIRA_USER_EMAIL="your-jira-email"

# Azure AD Configuration (https://learn.microsoft.com/en-us/azure/active-directory/develop/quickstart-register-app)
# only required for local dev if you run npm run dev:auth
AZURE_AD_CLIENT_ID="your-azure-client-id"
AZURE_AD_CLIENT_SECRET="your-azure-client-secret"
AZURE_AD_TENANT_ID="your-azure-tenant-id"

# JWT signing secret key
# Generate this using: npx auth secret
NEXTAUTH_SECRET="your-nextauth-secret-key"